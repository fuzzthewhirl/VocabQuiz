# VocabQuiz Docs (Local Preview)

This site is a GitHub Pages Jekyll site served from `docs/`.

## Run locally

1) Install dependencies:

```bash
bundle install
```

2) Start the server:

```bash
bundle exec jekyll serve --source docs
```

3) Open the site:

```
http://127.0.0.1:4000/
```
